services:
  - serviceId: explorer-uss
    title: USS Explorer
    description: IBM z/OS Unix Files UI service
    catalogUiTileId:
    instanceBaseUrls:
      - https://${ZOWE_EXPLORER_HOST}:${USS_EXPLORER_UI_PORT}/
    homePageRelativeUrl:
    routedServices:
      - gatewayUrl: ui/v1
        serviceRelativeUrl: {{ configs.baseUri }} # Note - this needs reading from manifest.yaml/component configs